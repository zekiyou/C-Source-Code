指针阅读：
1.从最里层未定义标识符开始
2.先往右看，再往左看
3.遇到方括号，圆括号,确定类型，掉头




int (*p2[5])(int *)



1.p2[5]  p2为数组

2.*p2[5]  p2为数组，数组元素是指针

3.(*p2[5])(int *) p2为数组，数组元素是函数指针，参数类型为int*

4.int (*p2[5])(int *) p2为数组，数组元素是函数指针，参数类型为int*，返回值类型为int