#include <stdio.h>

int main()
{
    int i=0;
    int j=++i+++i+++i;

    //编译器处理符号使用贪心算法
    // +  ==> ++(前置运算符 合法) ==> ++i
    // ++i+ (++i+? 要是+一个数就合法 贪心继续读)
    // ++i++ (开始处理 ++i++ ==> 1++ error)

    int j=++i + ++i + ++i;
    //空格作为C语言中的一个完整休止符
    //编译器读入空格时，对之前读入的符号进行处理
    //+ ==> ++ ==> ++i ==>++i 空格(开始处理)

    return 0;
}
